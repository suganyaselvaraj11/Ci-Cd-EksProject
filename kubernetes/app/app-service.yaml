apiVersion: v1
kind: Service
metadata:
  name: vprofile-app-service
  namespace: vprofile
spec:
  type: NodePort
  selector:
    app: vprofile-app
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30007